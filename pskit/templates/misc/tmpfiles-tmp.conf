# Managed by pskit. Overrides /usr/lib/tmpfiles.d/tmp.conf (30 days) with the
# same rule and a shorter age. Forgotten temporary files can fill the root
# disk; 30 days is too loose when agents write all day. /tmp is still emptied at boot.
D /tmp 1777 root root {{tmp_age}}
