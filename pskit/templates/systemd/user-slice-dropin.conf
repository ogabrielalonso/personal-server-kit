# Managed by pskit. Ceiling for the owner's sessions (agents, builds), so
# a runaway session cannot take the memory the brain and the system need.
[Slice]
MemoryAccounting=yes
MemoryHigh={{user_high}}
MemoryMax={{user_max}}
MemorySwapMax={{swap_max}}
